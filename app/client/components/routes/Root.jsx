Root = React.createClass({
    render() {
        return (
            <main className="animated fadeIn root view">
                TODO: Replace with Menu
                <Weather />
            </main>
        );
    }
});